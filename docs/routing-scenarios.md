# Routing inspection A–H

These are manual contract inspections, not claims of eight live model executions.
Each case was traced through SKILL.md classification, routing and checkpoint rules.

| Case | Selected path | Excluded work | Result |
| --- | --- | --- | --- |
| A: Login → Masuk | SIMPLE, local edit + relevant check | no new plan, memory, graph, brainstorm or subagent unless ticket already requires plan | PASS |
| B: intermittent duplicate charge | COMPLEX, plan, systematic-debugging, targeted evidence, regression check, checkpoint | no speculative patch or broad memory dump | PASS |
| C: vague dashboard | COMPLEX, plan, clarify product ambiguity/brainstorm, UI/UX Pro Max | Taste only for compatible visual value; no automatic duplicate UI lifecycle | PASS |
| D: why A depends on B | current context, existing graph or targeted references/source | no full scan or new graph by habit | PASS |
| E: continue @plan | checkpoint → status/source drift → NEXT ACTION | no bootstrap restart or repeating valid DONE phases | PASS |
| F: local TypeScript typo | SIMPLE native file edit + suitable compiler/targeted check | no filesystem/Git/docs/reasoning MCP stack | PASS |
| G: failed browser request | one browser surface, DevTools only for distinct console/network need | no browser A/B ping-pong | PASS |
| H: review finished implementation | targeted diff, code-review-and-quality, relevant evidence | no reimplementation absent defect | PASS |

Additional inspection: missing memory service does not block E; contradictory current
source updates only stale checkpoint facts. Existing ticket plan is retained even when
the next edit is SIMPLE. Explicit single UI skill remains respected. A missing specialist
falls back to its method, not an installation spree.

## Rescue routing contract inspection

These additions refer to rescue scenario IDs, not the unrelated A-H cases above.
They describe written routing expectations; actual host/implicit selection remains
UNVERIFIED. Inspect the [canonical route](../skills/andino-workflow/references/routing.md)
and [rescue contract](../skills/ai-codebase-rescue/SKILL.md) for their boundaries.

| Case | Expected path | Evidence / lifecycle requirement | Failure signal |
| --- | --- | --- | --- |
| Rescue A: clean repo and one simple local bug | Ordinary local fix; systematic diagnosis only if root cause is unknown. | Targeted evidence and relevant verification; retain an existing ticket plan if present. | Broad rescue audit or replacement merely because a bug exists. |
| Rescue B: patch-heavy bounded subsystem with repeated regressions | Targeted eligibility inspection, then ai-codebase-rescue when justified. | Reproduction, known contracts/consumers and regression protection; choose disposition from evidence. | AI provenance selects remediation or repeated failures automatically mean REMAKE. |
| Rescue J: existing Andino execution plan | Specialist binds to and reuses that plan/checkpoint. | Check current repository drift and return findings/verification to Andino. | Parallel rescue plan, independent DONE or authoritative NEXT ACTION. |
| Explicit ai-codebase-rescue invocation | Specialist may activate subject to native invocation controls; still assess eligibility. | Reuse Andino lifecycle, scope and authorization; absent plan uses normal Andino contract for non-trivial work. | Direct invocation bypasses gates, lifecycle or permissions. |
| Vague "AI wrote this / clean this" | Small targeted eligibility inspection; exit to ordinary work or KEEP when appropriate. | Concrete engineering condition, not provenance or aesthetics; material uncertainty gets a specific evidence action. | Automatic rescue, REMAKE or full-repository rewrite. |

## Requesting bounded rescue

Describe the target, symptom, preserved behavior and existing plan if available:
"Stabilize the payment retry subsystem after repeated regressions; preserve the
public API and use the existing Andino execution plan." A production-hardening
request should identify the fragile prototype and relevant risks; it is not a
request to audit every domain or replace the repository.

Where the host exposes the skill, explicitly select `ai-codebase-rescue` using that
host's native mechanism (for example, `$ai-codebase-rescue` in Codex). Explicit
selection does not authorize REMAKE, publication or live-data changes by itself.
Andino owns the checkpoint, acceptance and authoritative NEXT ACTION; the specialist
returns evidence and a recommendation. Normal small bugs need no rescue invocation.

This documents canonical source behavior only. This integration ticket does not
install the rescue skill, change host invocation policy or prove implicit discovery.
If unavailable, follow Andino's existing availability/invocation boundaries without
claiming a skill was invoked or installing it automatically.
